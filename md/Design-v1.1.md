# 紫葱酱的欧冠 Fantasy 赛程规划器 — 产品设计文档（Design.md v1.1 · 增量修订）

> **版本**：v1.1（基于 v1.0 的**增量修订**，仅列出需要变更的章节；未列出的章节视为维持 v1.0 不变）
> **性质**：本文件只描述「相对 v1.0 的变更」与「新增规范」，不含任何代码/React 实现。
> **目标读者**：前端工程师、UI/UX 设计师、后端数据工程师
> **核心变更主线**：赛程矩阵的信息架构从「开球时间槽（Kickoff Slot）双层结构」重构为「自然日期（北京时间）单层结构」，并随之引入北京时间（UTC+8）统一、主客场文本化、真实表格化、Team Selector 勾选面板、Captain Route 浮层化。

---

## 0. 修订总览（先给结论）

| # | 修订项 | 涉及章节 | 一句话变更摘要 |
| --- | --- | --- | --- |
| 1 | 废弃 Kickoff Slot 双层结构 | §0 / §1 / §3 / §12 / §13 / §14 / §16 / §17 | 矩阵行由「日期 + 时间槽」双层，改为「自然日期（北京时间）」单层 |
| 2 | 统一北京时间 UTC+8 | §0 / §12 / §15 / §17 + 新增「时区转换规则」 | 所有开球时间、星期、日期范围一律按北京时间展示与分组 |
| 3 | 主客场文本化 | §0 / §12 / §16 | 单元格由「图标 + 缩写」改为文本 `NAP (H)` / `POR (A)` |
| 4 | 真实表格化 | §12 / §16 | 矩阵改为「卡片容器 + 语义表格」结构，而非卡片格网 |
| 5 | 新增 Team Selector Panel | §0 / §3 / §4 / §5 / §14 / §16 + 新增「Team Selector 规范」 | 36 队勾选框面板，与搜索双向联动 |
| 6 | 球队列头单行化 | §12 / §16 | 列头由「全称 + 缩写」改为单行 `ARS` |
| 7 | Captain Route 改为 Overlay | §0 / §12 / §14 / §16 + 新增「Captain Route 规范」 | 不再重构/重排矩阵，改为浮层 + 单元格角标 |

**修改原因（一句话概括）**：

1. **按自然日期组织**：欧冠每轮比赛高度集中在 2~3 天，但玩家「先看哪天有比赛、再比谁先踢」的认知顺序是**日期在前、时间在后**；旧结构把「时间槽」提到行级，导致一行代表一个槽、同一天的比赛被拆散成多行，扫读时要纵向跨行对比，成本高。
2. **北京时间 UTC+8**：目标用户为国内 Fantasy 玩家，欧洲当地时间（CET/CEST）的「周中夜场」在北京已是**次日凌晨**；若不转换，日期/星期与用户体感完全错位（详见 §1）。
3. **文本化主客场**：`图标 + 缩写` 在紧凑性上有优势，但 `(H)/(A)` 文本是中文玩家更熟悉、零学习成本的语义；且真实表格化后单元格有足够空间承载两个短文本。
4. **真实表格化**：日期对齐、列对齐是「扫读赛程」的核心诉求，语义表格天然保证行列对齐与屏幕阅读器可访问性；外层保留卡片容器以维持「轻量游戏感」。
5. **Team Selector**：36 队逐一到搜索框输入是低效的；勾选面板让「多选对比」从 10 次交互降到 1~2 次，且是搜索结果的天然承载容器。
6. **列头单行化**：矩阵最多 10 列，列头越窄越能容纳更多列；`ARS` 三字码本身即唯一标识，全称交给 tooltip。
7. **Captain Route 浮层化**：队长路线的本质是「按时间排序 + 找可切换对」，这是**派生视图**而非主视图；旧方案在开启时重排矩阵行，破坏用户已建立的日期空间记忆，浮层化能在不打断主视图的前提下叠加该视图。

---

## 1. 时区转换规则（新增 · 全局生效）

### 1.1 规则

- **源时区**：欧洲中部时间（CET/CEST），IANA 标识建议 `Europe/Paris`（自动处理夏令时）。⚠️ 接入前须先确认源数据 `dateTime` 确为欧洲本地时间（本节基于「数据为欧洲本地时间」这一假设撰写）。
- **展示时区**：北京时间 `Asia/Shanghai`（UTC+8，全年无夏令时）。
- **转换方式**：将源字符串解析为「欧洲本地时间」→ 经 IANA 时区库转换为 `Asia/Shanghai` → 得到**北京开球时间**与**北京自然日期**。禁止对偏移做「手写 +6/+7」的常量硬编码，夏令时边界必须由时区库判定。

### 1.2 偏移与夏令时边界

2026 年欧洲夏令时（CEST, UTC+2）于 **10 月最后一个周日（10/25）** 结束，此后为冬令时（CET, UTC+1）。故本轮欧冠 8 个比赛日的偏移**并非恒定**：

| 赛季阶段 | 欧洲时区 | 与北京时差 | 18:45 开球 → 北京 | 21:00 开球 → 北京 |
| --- | --- | --- | --- | --- |
| MD1–MD3（09/08–10/21，夏令时） | CEST（UTC+2） | **+6h** | 次日 00:45 | 次日 03:00 |
| MD4–MD8（11/03–01/27，冬令时） | CET（UTC+1） | **+7h** | 次日 01:45 | 次日 04:00 |

### 1.3 关键影响：绝大多数比赛落在「次日凌晨」

以 MD1 为例（源数据为 CEST）：

| 源数据（欧洲本地） | 北京时间（UTC+8） | 北京自然日期 | 北京星期 |
| --- | --- | --- | --- |
| 09/08 21:00 | 09/09 03:00 | 09/09 | 周三 |
| 09/09 18:45 | 09/10 00:45 | 09/10 | 周四 |
| 09/09 21:00 | 09/10 03:00 | 09/10 | 周四 |
| 09/10 21:00 | 09/11 03:00 | 09/11 | 周五 |

**结论**：一个「欧洲跨度 3 天（09/08–09/10）」的 Matchday，在北京时间下实际分布在 **09/09（周三）、09/10（周四）、09/11（周五）** 三个自然日期，且全部为凌晨时段。这正是「按自然日期（北京时间）组织矩阵行」的必要性所在——若按欧洲日期展示，用户看到的「周三 21:00 的比赛」实际是北京「周四凌晨 3 点」。

### 1.4 设计约束（贯穿全文档）

1. 矩阵**行键 = 北京自然日期**；Accordion 标题的日期范围、星期、单元格时间均为北京时间。
2. 「星期」按**北京日期**计算（欧洲 09/09 的夜场，北京已是 09/10 周四）。
3. 所有界面时间需明确标注「北京时间」或「UTC+8」，避免与欧洲时间混淆（Footer / Accordion 标题可加一行小字说明）。
4. 数据层新增派生字段（见 §15）：`dateTimeBeijing`、`dateKeyBeijing`、`weekdayBeijing`、`kickoffBeijing`。

---

## 2. Fixture Matrix 设计规范（更新后，替换 v1.0 §12）

### 2.1 矩阵结构（按自然日期 · 真实表格）

- **行**：**自然日期（北京时间）**，一行一个日期（如「周四 09/10」）。同一天内的多场比赛**不再按时间槽拆行**。
- **列**：已选球队（≤10），列头为**单行三字码** `ARS`。
- **单元格**：该队在该北京日期的比赛，格式 `对手 (H/A)` + 次要时间；无比赛留白。
- **容器结构**：外层的 Matchday Accordion 是卡片容器（`radius-lg` + `shadow-md` + 圆角），卡片内部是一个**语义表格**（`<table>`：`<caption>` / `<thead>` / `<tbody>`），用真实行列表义而非卡片格网，保证列对齐与屏幕阅读器可访问性。

```
┌────────────────────────────────────────────────────────────┐  ← 卡片容器（Accordion 展开态）
│ <caption> Matchday 1 · 09/09~09/11 北京时间                │
│ ┌────────────┬──────┬──────┬──────┬──────┬──────┐          │
│ │ 日期(北京) │ ARS  │ MCI  │ BAR  │ BAY  │ PSG  │ ← thead  │
│ ├────────────┼──────┼──────┼──────┼──────┼──────┤          │
│ │ 周三 09/09 │      │POR(A)│      │      │      │          │
│ │ 周四 09/10 │NAP(A)│      │FEY(H)│      │SBR(H)│ ← tbody  │
│ │ 周五 09/11 │      │      │      │BOD(H)│      │          │
│ └────────────┴──────┴──────┴──────┴──────┴──────┘          │
└────────────────────────────────────────────────────────────┘
```

### 2.2 单元格格式（主客场文本化）

对 v1.0 §12.2 的格式对比结论做**修正**：主方案由「自绘 SVG 图标 + 缩写」改为**文本 `对手 (H)` / `对手 (A)`**。

| 方案 | 示例 | 结论 |
| --- | --- | --- |
| 旧推荐：图标 + 缩写 | `▉ NAP` / `▢ INT` | 弃用（学习成本高于文本；表格化后无紧凑性红利） |
| **新推荐：文本主客场** | `NAP (H)` / `POR (A)` | **推荐**（零学习成本、语义显式、便于表格内纵向扫读） |

- **主客场语义**：用 `(H)` / `(A)` 文本后缀显式表达，颜色作为**辅助**编码（`(H)` 用 `primary` 底纹或深色，`(A)` 用浅底 + `primary` 描边），两者并列，避免仅靠颜色。
- 不出现 `vs` / `@` 等冗余文字（`(H)/(A)` 已覆盖主客语义）。
- 通过 `title` / `aria-label` 补充完整文本（如 `Arsenal vs Napoli · Matchday 1 · 2026-09-10 03:00 北京时间 · 客场`）。

### 2.3 单元格结构（两行：对手 + 次要时间）

```
主场：┌────────────┐      客场：┌────────────┐
      │ FEY (H)    │            │ NAP (A)    │
      │  00:45     │            │  03:00     │
      └────────────┘            └────────────┘
      （primary 底纹/深色）       （浅底 + primary 描边）
```

- **首行**（主体）：`对手缩写 (H/A)`，Caption 600 字重。
- **次行**（次要信息）：北京开球时刻 `HH:mm`，Small 字号 + `text-muted` 色。时间为**次要信息**，不作行分组依据，仅用于冲突判定与队长路线排序。
- **尺寸**：最小高度 48px（两行），内边距 8px，圆角 `radius-sm`；时间使用 `tabular-nums` 对齐。
- **无比赛**：留白（不显示 `-` / `N/A` / `No Match`）。

### 2.4 Hover 信息（Tooltip）

悬停格子显示浮层（延迟 200ms），字段较 v1.0 增加**北京时间**：

```
┌─────────────────────────────┐
│  Napoli vs Arsenal          │   ← 该队视角（客场队则主队视角切换）
│  Matchday 1                 │
│  2026-09-10（周四 · 北京时间）│
│  03:00                      │
│  Away                       │
└─────────────────────────────┘
```

### 2.5 冲突识别（撞车 · 更新）

- **「撞车」的新定义**：同一**北京自然日期**内、**开球时刻相同**的 ≥2 支已选球队 → 冲突。
  - 例：MD1 周四 09/10，ARS 客场 NAP @ 03:00 与 PSG 主场 SBR @ 03:00 → **撞车**（同 03:00）。
  - 同日但不同时刻（如 BAR @ 00:45 与 ARS @ 03:00）→ **错开**，是 Captain Switch 的候选对。
- **视觉编码**：冲突格叠加 `warning` 色角标/描边；错开格不标。语义不能仅靠颜色，须配文字角标（如角标内「撞」或 tooltip 说明）。
- 对比旧逻辑：旧版以「时间槽」为聚类单位，新版以「日期 + 时刻」为聚类单位，判断更精确（同一天内 00:45 与 03:00 不再被误判为同一槽）。

### 2.6 队长路线（Captain Route）

- 从矩阵内**移除**（不再重排矩阵行）。改为**浮层 Overlay + 单元格角标**，完整规范见 §4「Captain Route 规范」。
- 矩阵侧仅承担：开启时给单元格叠加**开球先后序号角标**（①②③，按北京时刻升序），不改变行/列结构与位置。

---

## 3. Team Selector 规范（新增组件）

### 3.1 定位与职责

- **定位**：选队的**主通道**。替代 v1.0 的「纯搜索下拉建议」作为多选的主要入口（下拉仅保留为键盘快速选中的辅助）。
- **位置**：位于「搜索区」下方、与「已选 Tag 区」相邻的**可折叠面板**。默认收起为 `▸ 展开 36 队选择（已选 5/10）` 按钮；展开后展示全部 36 队的勾选列表。
- **桌面优化项**：可改为左侧 sticky 侧栏，让勾选面板与矩阵并排常驻（见 §6.1 Wireframe）。

### 3.2 结构

```
┌────────────────────────────────────────────┐
│ ▾ 收起 36 队选择              已选 5 / 10  │
├────────────────────────────────────────────┤
│  ☑ Arsenal    (ARS)   Pot 1               │
│  ☐ Monaco     (MON)   Pot 1               │
│  ☐ Milan      (MIL)   Pot 1               │
│  ☑ Man City   (MCI)   Pot 1               │
│  ...  （可按 Pot / 首字母 / 国家联赛分组）    │
└────────────────────────────────────────────┘
```

- 每行：`[checkbox]` + 队名全称 + `(缩写)`；可选队徽小图标与分组标签（Pot）。
- 勾选态：checkbox 实心 + 行背景 `accent-light`。

### 3.3 交互契约

1. **勾选 / 取消**：勾选 = 加入已选（同步到「已选 Tag 区」与矩阵列）；取消 = 移除。所有入口（勾选框、Tag 删除、清空）读写**同一数据源 `selectedCodes`**。
2. **与搜索双向联动**：
   - 搜索框输入（防抖 150ms）→ 面板**实时过滤**，仅显示匹配队（按缩写/全称模糊匹配），勾选态保留不清空。
   - 面板勾选结果 → 实时反映到已选 Tag 区（反向同步）。
   - 搜索命中唯一且已聚焦时，回车可直接勾选该队（键盘快捷路径）。
3. **上限**：满 10 队后，其余 checkbox 置灰禁用，并提示「最多同时展示 10 支球队」。
4. **空结果**：搜索无匹配时，面板显示空态文案（如「未找到相关球队」）。

### 3.4 状态机

```
collapsed ──(点击「展开」)──▶ expanded
expanded  ──(点击「收起」/ 面板外区域)──▶ collapsed

（面板内部行状态）
unchecked ⇄ checked   （受 selectedCodes 与 10 队上限约束）
```

---

## 4. Captain Route 规范（更新后 · Overlay 化）

### 4.1 核心原则

- **不改结构**：开启「队长路线」后，矩阵的行/列/单元格**位置完全不变**，不重排、不拆分、不折叠。
- **两条通道叠加**：
  1. **矩阵角标**：给有比赛的单元格叠加开球先后序号角标（①②③…），按**北京时刻升序**、以**每个自然日期**为排序单元。
  2. **浮层 Overlay**：一个浮层面板，按比赛日列出「开球时间线」与「可切换对」结论，供快速扫读。

### 4.2 序号规则

- 序号在**同一北京日期内**按开球时刻升序编列；不同日期之间序号连续（跨日期也体现先后）。
- 同日期同时刻的多队共享同一序号（即「撞车」的多队为并列序号，如 `③③`）。
- 例（MD1 已选 ARS/MCI/BAR/BAY/PSG）：

| 序号 | 北京日期 | 时刻 | 球队 |
| --- | --- | --- | --- |
| ① | 周三 09/09 | 03:00 | MCI（客 POR） |
| ② | 周四 09/10 | 00:45 | BAR（主 FEY） |
| ③ | 周四 09/10 | 03:00 | ARS（客 NAP）、PSG（主 SBR）← 并列 |
| ④ | 周五 09/11 | 03:00 | BAY（主 BOD） |

### 4.3 浮层内容（Captain Route Panel）

```
┌─ 队长路线 · Matchday 1 ─────────────────┐
│ 周三 09/09                              │
│   ① 03:00  MCI  (客 vs POR)             │
│ 周四 09/10                              │
│   ② 00:45  BAR  (主 vs FEY)             │
│   ③ 03:00  ARS  (客 vs NAP)             │
│   ③ 03:00  PSG  (主 vs SBR)   ⚠ 撞车    │
│ 周五 09/11                              │
│   ④ 03:00  BAY  (主 vs BOD)             │
├─────────────────────────────────────────┤
│ 可切换对（先踢→后踢）                    │
│   MCI → BAR → (ARS | PSG) → BAY        │
└─────────────────────────────────────────┘
```

- **可切换对判定**：同一北京日期内、开球时刻不同的两队（先踢的做队长，赛后切到后踢的）；跨日期同样按先后顺序衔接。
- **撞车提示**：并列序号的球队（如 ARS/PSG）在浮层内标 `⚠ 撞车`，提示二者不可互相切换（同刻踢球）。
- **交互**：浮层悬停/点击某条路线，矩阵对应单元格高亮联动；关闭开关则浮层与角标同时消失。

### 4.4 与矩阵角标的联动

- 开启开关 → 矩阵单元格左上角出现小号序号徽标（`primary` 底、白字）。
- 序号徽标仅是「时间先后」信号，撞车等语义仍需颜色 + 文字双编码（浮层内已有文字说明）。

---

## 5. 其余受影响章节的增量修订

### 5.1 §0 文档导读（替换第 2/3/4 条）

1. （不变）
2. ~~图标 + 缩写~~ → **单元格采用文本主客场 `NAP (H)` / `POR (A)`**，理由是零学习成本、语义显式（详见 §12.2）。
3. **「冲突」的本质是「同一北京时间自然日期内的开球时刻」**：矩阵按自然日期组织，冲突与队长切换建立在「日期 + 时刻」的聚类上，而非时间槽。
4. **移动端不做横向矩阵**：降级为「按自然日期纵向排列的时间线卡片」（不再按时间槽分组，详见 §13.4）。

### 5.2 §1.1 产品目标（微调）

四项判断中，「谁先踢、谁后踢」降级为**日期内的次要信息**，主判断改为：

- **哪天有比赛**（按北京日期扫描）
- **谁和谁撞车**（同一北京日期内、同时刻开球）
- **谁先踢、谁后踢**（日期内按时刻排序，配合 Captain Route 浮层）
- **赛程错开的球队**（不同时刻/不同日期的 Captain Switch 候选）

### 5.3 §3 信息架构（结构树 + 层级）

```
紫葱酱 Fantasy 赛程规划器（单页应用）
│
├── Header（品牌 + 全局操作：队长路线开关 / 清空选择）
├── Search Area（搜索框，联动 Team Selector 过滤）
├── Team Selector Panel（36 队勾选面板，可折叠，主选队通道）
├── Selected Teams Area（已选球队 Tag 列表）
├── Fixture Matrix Area（核心区）
│   └── Matchday Accordion × 8
│       └── Schedule Table（北京日期 × 球队 真表格）
│   └── Captain Route Overlay（开启时浮层叠加，不重构矩阵）
└── Footer（数据来源 / 免责声明 / 时区说明）
```

**§3.2 层级关系**改为：

- 球队（Team）与 **北京自然日期（Date）** 是两个正交维度，构成矩阵的行列。
- 比赛（Match）是矩阵单元格内容。
- 开球时刻（Kickoff Time）是**单元格级属性**，用于冲突判定与队长路线排序，不再是行级分组维度。

### 5.4 §4 页面结构（新增/修改）

- **§4.2 Search Area**：搜索框语义改为「过滤 Team Selector + 键盘快捷勾选」，而非唯一选队入口。
- **新增 §4.3 Team Selector Panel**：见 §3 规范。
- **原 §4.3 Selected Teams Area**：不变（Tag 列表，与勾选面板双向同步）。
- **§4.4 Fixture Matrix Area**：Accordion 展开后渲染「卡片容器 + 语义表格」，行按北京日期；Captain Route 以浮层叠加。
- **§4.1 Header**：`队长路线` 开关的说明由「矩阵按时间槽高亮」改为「开启浮层 + 角标」。

### 5.5 §5 组件设计（组件清单更新）

| 组件 | 职责变更 |
| --- | --- |
| `TeamSearch` | 不变（搜索 + 模糊匹配），但下拉降级为辅助，主导作用为过滤 Selector |
| `TeamSelector`（新增） | 36 队勾选面板，承载多选主通道，与搜索/已选联动 |
| `TeamTag` / `SelectedTeams` | 不变 |
| `MatchdayAccordion` | 不变（日期范围显示改为北京时间） |
| `FixtureMatrix` | 改为渲染「语义表格」，行=北京日期；状态 `default / captain-route-overlay` |
| `FixtureCell` | 格式改为 `对手 (H/A)` + 次要时刻 |
| `CaptainRouteOverlay`（新增） | 浮层面板，展示开球时间线 + 可切换对 + 撞车提示 |
| `CaptainRouteToggle` | 不变（开关 off/on，控制 overlay + 角标） |
| `Legend` | 图例更新：`(H) 主场` / `(A) 客场` / `同刻撞车` / `序号=开球先后` |

### 5.6 §13 Responsive（移动端改按日期）

- **§13.2 Desktop / §13.3 Tablet**：语义表格，冻结表头与冻结首列（日期列），横向滚动逻辑不变。
- **§13.4 Mobile**：竖向时间线**改按「自然日期」纵向分组**（不再按时间槽）：
  - 日期标题：`周四 · 09/10（北京时间）`
  - 日期内每张卡片：`[ARS] NAP (A) · 03:00`（文本主客场 + 时刻）
  - Captain Route 浮层在移动端降级为**底部 Sheet**，矩阵角标保留。

### 5.7 §14 UX Flow（流程更新）

- **§14.2 选择球队**：主路径改为「展开 Team Selector → 勾选」，搜索仅用于过滤。
- **§14.3 理解矩阵**：图例改为解释 `(H)/(A)`、时间、撞车角标、序号。
- **§14.4 发现冲突**：同一北京日期内同时刻的格子标 `warning` 角标。
- **§14.5 发现队长路线**：开启开关 → 单元格出现序号角标 + 浮层列出时间线与可切换对；矩阵结构不变。

### 5.8 §15 数据结构（新增北京时间派生字段）

`Match` 在 v1.0 §15.2 基础上**新增**以下派生字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `dateTimeBeijing` | string(ISO 8601, +08:00) | 北京开球时间 |
| `dateKeyBeijing` | string `YYYY-MM-DD` | **北京自然日期（矩阵行键）** |
| `weekdayBeijing` | string | 北京星期（周几） |
| `kickoffBeijing` | string `HH:mm` | 北京开球时刻（单元格次要时间） |

`Matchday` 的 `dateRange` 改为**北京日期范围**；`slots`（v1.0 §15.3）废弃为行分组维度，仅保留「同日同时刻」冲突判定所需的派生值。

> ⚠️ 关键工程提示：源数据 `dateTime` 为 `MM/DD/YYYY HH:mm:ss`（无时区标记），归一化时须按「欧洲本地时间」解析后转换（§1），而非直接 `new Date()`。

### 5.9 §17 Roadmap（实现优先级更新）

1. 数据归一化层：解析 `fixtures_90_en.json` → 扁平结构 + **时区转换（CET/CEST → 北京时间）** + 北京日期派生（§1、§15）。
2. 球队搜索 + **Team Selector 勾选面板** + 多选 Tag（10 队上限）。
3. 赛程矩阵渲染：Accordion + **语义表格** + 单元格（`对手 (H/A)` + 时刻）。
4. 冲突识别：按「北京日期 + 时刻」聚类。
5. **Captain Route Overlay**：时间线 + 序号角标 + 可切换对。
6. 响应式 + 移动端按日期时间线。
7. 空状态 + 热门球队推荐 + 图例。

§17.2 技术注意点第 1 条「时区」改为：**源数据欧洲时间 → 北京时间 UTC+8**，且须用 IANA 时区库处理夏令时边界（不可手写固定偏移）。

---

## 6. 新版 Wireframe（线框图）

### 6.1 Desktop

```
┌────────────────────────────────────────────────────────────────────────────────┐
│  🧅 紫葱酱 Fantasy 赛程规划器                [队长路线 ⚪]  [清空全部]        │
├────────────────────────────────────────────────────────────────────────────────┤
│  🔍 [ 搜索球队名或缩写，如 ARS / Arsenal …                         ]        │
│  ▸ 展开 36 队选择（已选 5 / 10）                                             │
│  ┌──────────────┬──────────────┬──────────────┬──────────────┐               │
│  │ ☑ Arsenal ARS│ ☐ Monaco MON │ ☐ Milan MIL  │ ☐ …          │  ← 勾选面板   │
│  └──────────────┴──────────────┴──────────────┴──────────────┘               │
├────────────────────────────────────────────────────────────────────────────────┤
│  已选：[ARS ×] [MCI ×] [BAR ×] [BAY ×] [PSG ×]            （最多 10 支）       │
├────────────────────────────────────────────────────────────────────────────────┤
│  ▼ Matchday 1                      09/09 ~ 09/11（北京时间）                  │
│  ┌──────────────────────────────────────────────────────────────────────────┐ │
│  │ 日期(北京) │  ARS  │  MCI  │  BAR  │  BAY  │  PSG  │                     │ │
│  ├────────────┼───────┼───────┼───────┼───────┼───────┤                     │ │
│  │ 周三 09/09 │       │POR (A)│       │       │       │                     │ │
│  │            │       │ 03:00 │       │       │       │                     │ │
│  │ 周四 09/10 │NAP (A)│       │FEY (H)│       │SBR (H)│  ← ARS/PSG 撞车      │ │
│  │            │ 03:00 │       │ 00:45 │       │ 03:00 │     （同 03:00）     │ │
│  │ 周五 09/11 │       │       │       │BOD (H)│       │                     │ │
│  │            │       │       │       │ 03:00 │       │                     │ │
│  └──────────────────────────────────────────────────────────────────────────┘ │
│  ▶ Matchday 2                                                                │
├────────────────────────────────────────────────────────────────────────────────┤
│  数据来源说明 · 非官方 · 仅供 Fantasy 参考 · 时间为北京时间（UTC+8）          │
└────────────────────────────────────────────────────────────────────────────────┘

   队长路线开启后，浮层叠于矩阵右侧（不改变矩阵结构）：
   ┌─ 队长路线 · MD1 ──────┐
   │ 周三 09/09            │
   │  ① 03:00 MCI(客POR)   │
   │ 周四 09/10            │
   │  ② 00:45 BAR(主FEY)   │
   │  ③ 03:00 ARS(客NAP)   │
   │  ③ 03:00 PSG(主SBR) ⚠ │
   │ 周五 09/11            │
   │  ④ 03:00 BAY(主BOD)   │
   │ 可切换：MCI→BAR→      │
   │   (ARS|PSG)→BAY       │
   └───────────────────────┘
```

### 6.2 Mobile（按自然日期的时间线）

```
┌──────────────────────────────┐
│ 🧅 紫葱酱 Fantasy 规划器      │
│ 🔍 搜索球队…                  │
│ ▸ 展开 36 队选择             │
│ [ARS ×] [MCI ×] [BAR ×] +2  │
├──────────────────────────────┤
│ ▼ Matchday 1  (北京时间)     │
│  ┌─ 周三 · 09/09 ─────────┐  │
│  │  MCI   POR (A) · 03:00 │  │
│  ├─ 周四 · 09/10 ─────────┤  │
│  │  BAR   FEY (H) · 00:45 │  │
│  │  ARS   NAP (A) · 03:00 │  │  ⚠ 与 PSG 撞车
│  │  PSG   SBR (H) · 03:00 │  │
│  ├─ 周五 · 09/11 ─────────┤  │
│  │  BAY   BOD (H) · 03:00 │  │
│  └────────────────────────┘  │
│ ▶ Matchday 2                 │
├──────────────────────────────┤
│ 队长路线：底部 Sheet 浮层     │
└──────────────────────────────┘
```

---

## 7. 变更范围边界（明确「不改」）

- **不改代码**：本次为文档修订，v1.0 已实现的 Vite/React 代码（`FixtureMatrix` / `Timeline` 等组件仍按旧的时间槽结构）**本次不修改**，其对齐 v1.1 的代码改造留待后续开发任务。
- **不改的设计系统**：§6 视觉理念、§7 色彩、§8 字体、§9 间距、§10/§11 卡片与 Accordion 规范维持 v1.0 不变（仅 Legend 文案与单元格格式受影响）。
- **不改数据源**：`fixtures_90_en.json` 结构不动，仅在归一化层新增时区转换与北京日期派生。
