# 紫葱酱的欧冠 Fantasy 赛程规划器 — 产品设计文档（Design.md v1.2 · 增量修订）

> **版本**：v1.2（基于 v1.1 的**增量修订**，仅列出需要变更/新增的章节；未列出的章节维持 v1.1 不变）
> **性质**：本文件只描述「相对 v1.1 的变更」与「新增规范」，不含任何代码 / React / TypeScript / Tailwind 实现。
> **目标读者**：前端工程师、UI/UX 设计师、后端数据工程师
> **核心变更主线**：将比赛日分组逻辑从「北京自然日期字符串」升级为「GDID 内部键 + 比赛日序数显示名」；新增 Matchday Range Filter；将 Captain Route 明确定位为「比赛日顺序分析工具（分析层）」，进一步收敛其边界；为 Fixture Matrix 单元格预留可扩展的「扩展槽」，兼容未来的 FDR / 球队评分 / 赛程评分等维度。

---

## 0. 修订总览（先给结论）

| # | 修订项 | 涉及章节 | 一句话变更摘要 |
| --- | --- | --- | --- |
| 八 | 比赛日分组逻辑改用 GDID | §1（新）/ §2 / §5.8 | 矩阵行由「北京日期字符串」改为「GDID 分组」，行标签改为「比赛日一/二/三」，GDID 仅作内部键、前端禁止展示 |
| 九 | 新增 Matchday Range Filter | §3（新）/ §4 / §6 / §7 | 顶部「起始 MD → 结束 MD」双下拉，默认 MD1→MD8，越界自动纠正 |
| 十 | Captain Route 定位升级 | §4（替换 v1.1 §4） | 明确为「比赛日顺序分析工具」，是**分析层**而非数据层，绝不改变矩阵结构 |
| 十一 | Fixture Matrix 扩展预留 | §5（新）/ §2.3 | 单元格增加「扩展槽」，`NAP (H)` → `NAP (H) [FDR 2]` / `NAP (H) ★★`，可扩展承载未来评分维度 |

**四项修改的结论一句话版**：

1. **修改八**：矩阵行以 **GDID** 为内部分组键（同 GDID = 同比赛日 = 同行）；显示名「比赛日一/二/三」由**北京日期升序序数**自动生成，与 gdId 的全局值**解耦**（因 gdId 是赛季全局递增，MD2 的 gdId 是 4、5，不是 1、2）。前端任何界面**不得出现 "GDID" 字样**。
2. **修改九**：新增 **Matchday Range Filter**（起始/结束两个下拉），默认 MD1→MD8（全量），选定区间外不渲染；起始 > 结束时**自动交换**纠正。
3. **修改十**：Captain Route 是**「比赛日顺序分析工具」**——一个叠加在主视图之上的**分析层**，只做高亮 / 角标 / 路径提示，**不改动矩阵的行/列/单元格结构与位置**。
4. **修改十一**：单元格预留**扩展槽**，以徽标/标签形式承载未来维度（FDR、球队评分、赛程评分、攻防评分），扩展示增量式追加、不破坏现有两行单元格布局。

---

## 1. 比赛日分组规则（GDID 内部逻辑）— 修改八

### 1.1 核心结论（先给结论）

- **分组键**：一个 Matchday 内，**相同 GDID = 同一「比赛日」= 矩阵同一行**。
- **显示名**：`比赛日一` / `比赛日二` / `比赛日三`（本轮欧冠每个 Matchday 内最多 3 个比赛日，实际 1~3 个）。
- **显示名来源**：由**北京自然日期的升序序数**自动生成，**不是**「gdId 全局值 → 比赛日序数」的直接映射。
- **前端红线**：任何界面、任何 tooltip、任何 aria-label **禁止出现 "GDID" 或 "gdId" 字样**；GDID 只存在于数据层与分组逻辑内部。

### 1.2 GDID 字段说明（数据层事实）

源数据 `fixtures_90_en.json` 的每个比赛对象均含 `gdId` 字段（原始为字符串，如 `"1"`），语义为 **Game Day ID**——即「该比赛所属的自然比赛日」在**整个赛季全局**的递增编号。轮级另有 `mdId` / `gameday` / `gamedayNew` 字段标识 Matchday。

**关键事实：gdId 是赛季全局递增的，而非每轮从 1 开始。** 经真实数据核验，gdId 取值 1~16 依次覆盖 8 个 Matchday：

| Matchday | gdId 集合（全局） | 对应北京日期（已核验部分） | 本轮内「比赛日」序数 |
| --- | --- | --- | --- |
| MD1 | {1, 2, 3} | 09-09 / 09-10 / 09-11 | 比赛日一 / 比赛日二 / 比赛日三 |
| MD2 | {4, 5} | 10-14 / 10-15 | 比赛日一 / 比赛日二 |
| MD3 | {6, 7} | （同规则） | 比赛日一 / 比赛日二 |
| MD4 | {8, 9} | （同规则） | 比赛日一 / 比赛日二 |
| MD5 | {10, 11} | （同规则） | 比赛日一 / 比赛日二 |
| MD6 | {12, 13} | （同规则） | 比赛日一 / 比赛日二 |
| MD7 | {14, 15} | （同规则） | 比赛日一 / 比赛日二 |
| MD8 | {16} | 01-28 | 比赛日一 |

> 核验依据：以 MD1 为例，gdId=1 → 北京 2026-09-09、gdId=2 → 2026-09-10、gdId=3 → 2026-09-11；MD8 gdId=16 → 北京 2027-01-28。**每个 Matchday 内，gdId 与北京自然日期是 1:1 双射**（无两个 gdId 落在同一北京日期，也无一个 gdId 跨两个北京日期）。这一双射性质保证「按 GDID 分组」与「按北京日期分组」在结果上等价，但 GDID 作为内部键更稳健、且语义与「比赛日」直接对应。

### 1.3 分组规则

1. **取键**：对当前 Matchday 下的所有比赛，取 `gdId` 去重，得到该轮的比赛日集合 `G = {g1, g2, ..., gk}`（k ∈ {1,2,3}）。
2. **同键同行**：`gdId` 相同的比赛归入**同一行**（即同一「比赛日」）；不同 `gdId` 归入不同行。
3. **规范化**：原始 `gdId` 为字符串，归一化层须转成整数后再排序/分组（避免 `"10" < "2"` 的字典序错误）。

### 1.4 命名规范（比赛日一/二/三）

**显示名生成算法（前端，禁止暴露 GDID）**：

1. 取本轮比赛日集合 `G`；
2. 对每个 `g ∈ G` 取其对应的**北京自然日期** `dateKeyBeijing(g)`；
3. 对 `G` 按 `dateKeyBeijing` **升序**排序，得到序数 `1..k`；
4. 第 `j` 个比赛日的显示名 = `比赛日` + 中文序数（`一/二/三/...`），本轮最多到「三」；
5. 悬停 tooltip 补充完整上下文，如 `比赛日一 · 09/09（周三）· 北京时间`。

**为何「由北京日期排序序数生成」而非「gdId 直接映射」**：gdId 是赛季全局递增的，MD2 的 gdId 是 4、5 而非 1、2。若直接 `gdId → 比赛日序数`，MD2 会错误地显示「比赛日四 / 比赛日五」。因此必须**在本轮内按北京日期重新编序**，使每一轮都从「比赛日一」开始。

**「比赛日一/二/三」 vs 「Day 1/2/3」的取舍**：

| 方案 | 示例 | 优点 | 缺点 | 结论 |
| --- | --- | --- | --- | --- |
| **比赛日一/二/三** | 比赛日一 | 中文本土化，符合国内 Fantasy 玩家习惯；「一/二/三」序数直观；与上层英文「Matchday」形成**中英层级区分**，避免「Matchday vs Day」的英文语义重叠 | 与「Matchday」的中文译名「比赛日」存在措辞上的潜在重叠，需在上层保留英文「Matchday」或统一用「第 X 轮」规避 | **推荐** |
| Day 1/2/3 | Day 1 | 简洁 | 「Day」与「Matchday」同为英文且语义极近（Matchday 本身含 day），易混淆；中文界面夹英文不协调 | 备选 |

**推荐「比赛日一/二/三」**，并配套如下措辞约定以消除歧义：

- 上层（轮级）标签：**保留英文 `Matchday 1/2/…`**（延续 v1.1），或如未来全中文化则改称 `第 1 轮 / 第 2 轮`。
- 下层（比赛日级）标签：`比赛日一/二/三`。
- 两者层级分明：`Matchday 1` 内包含 `比赛日一 / 比赛日二 / 比赛日三`。

### 1.5 前端展示红线

- 任何界面、tooltip、图例、`aria-label`、`title`、空态文案中，**不得出现 "GDID" / "gdId" 字样**。
- 用户能感知到的只有两类文本：轮级 `Matchday N` 与比赛日级 `比赛日 N（中文序数）` + 对应北京日期。
- GDID 仅作为内部分组键存在于数据归一化层与渲染分组逻辑中。

---

## 2. Fixture Matrix 更新规范 — 修改八（替换 v1.1 §2 相关条目）

### 2.1 矩阵结构（行 = 比赛日）

- **行**：**比赛日**（GDID 分组），一行一个比赛日，行标签为 `比赛日一 / 比赛日二 / 比赛日三`。每个比赛日对应**唯一**一个北京自然日期（由 §1.3 双射保证）。
- **列**：已选球队（≤10），列头为单行三字码 `ARS`（**不变**）。
- **单元格**：该队在该比赛日的比赛，格式 `对手 (H/A)` + 次要时间；无比赛留白（**不变**）。
- **容器结构**：外层 Matchday Accordion 卡片容器 + 内部语义表格（`<table>`），**不变**。

**与 v1.1 的差异**：行级语义由「北京日期字符串」改为「比赛日（GDID 分组）」，行标签由「周三 09/09」改为「比赛日一」。行列正交关系、单元格格式、容器结构均不变。

```
┌────────────────────────────────────────────────────────────┐  ← 卡片容器（Accordion 展开态）
│ <caption> Matchday 1 · 09/09~09/11 北京时间                │
│ ┌────────────┬──────┬──────┬──────┬──────┬──────┐          │
│ │ 比赛日      │ ARS  │ MCI  │ BAR  │ BAY  │ PSG  │ ← thead  │
│ ├────────────┼──────┼──────┼──────┼──────┼──────┤          │
│ │ 比赛日一     │      │POR(A)│      │      │      │          │
│ │ (09/09)    │      │ 03:00│      │      │      │          │
│ │ 比赛日二     │NAP(A)│      │FEY(H)│      │SBR(H)│ ← tbody  │
│ │ (09/10)    │ 03:00│      │ 00:45│      │ 03:00│          │
│ │ 比赛日三     │      │      │      │BOD(H)│      │          │
│ │ (09/11)    │      │      │      │ 03:00│      │          │
│ └────────────┴──────┴──────┴──────┴──────┴──────┘          │
└────────────────────────────────────────────────────────────┘
```

### 2.2 行标签呈现

- **主标签**：`比赛日一 / 比赛日二 / 比赛日三`（Small 字号、`text-muted`）。
- **副标签**：对应北京日期 `MM/DD`（更小字号、弱化），与主标签同行或换行。
- **悬停**：行标签 tooltip 显示 `比赛日一 · 09/09（周三）· 北京时间`。

### 2.3 单元格结构与扩展槽（与修改十一联动）

单元格在 v1.1「两行（对手 + 次要时间）」基础上，预留**第三行「扩展槽」**，用于承载未来评分维度（详见 §5）。扩展槽默认空、不占视觉空间，有数据时才渲染。

```
┌────────────┐
│ FEY (H)    │  ← 首行：对手缩写 (H/A)
│  00:45     │  ← 次行：北京开球时刻
│ [FDR 2] ★★ │  ← 扩展槽（默认隐藏，有数据时显示，见 §5）
└────────────┘
```

---

## 3. Matchday Range Filter 设计 — 修改九

### 3.1 定位与职责

- **职责**：控制「展示哪些 Matchday」，是对 Fixture Matrix **轮级可见范围**的筛选器，不改动单轮内部的任何结构。
- **位置**：位于**搜索区与已选球队区之上**的顶部筛选栏（Header 之下、Search Area 之上），作为全局筛选入口。

### 3.2 UI 结构

```
┌────────────────────────────────────────────────────────────┐
│ 比赛日范围：  [ MD1 ▼ ]  →  [ MD8 ▼ ]        （默认 MD1→MD8）│
└────────────────────────────────────────────────────────────┘
```

- 两个下拉：`起始 Matchday`（Start）与 `结束 Matchday`（End），各自列出 `MD1 … MD8`。
- 中间以 `→` 或 `至` 连接，语义为「从 Start 到 End（含两端）」。

### 3.3 交互规则

1. **默认值**：`MD1 → MD8`，即展示全部 8 个 Matchday。
2. **区间过滤**：选定 `MD2 → MD4` 时，仅渲染 MD2、MD3、MD4，**隐藏 MD1、MD5、MD6、MD7、MD8**。
3. **越界自动纠正**：当用户把 Start 选到大于 End 时（如 `MD5 → MD2`），**自动交换**为 `MD2 → MD5`，不弹出报错、不阻断操作。
4. **空区间**：Start = End（如 `MD3 → MD3`）合法，仅展示单个 Matchday。
5. **与其它筛选联动**：Range Filter 只决定轮级可见性；Team Selector、搜索、Captain Route 等在其上正常叠加，互不影响。
6. **滚动/锚点**：若单轮内容较长（`overflow: auto`），筛选结果变化后回到该区间首个 Matchday 的顶部。

### 3.4 状态

```
start: 1..8  （默认 1）
end:   1..8  （默认 8）
约束：当 start > end 时，交换 start ↔ end（自动纠正）
可见轮集合：{ MD_i | start ≤ i ≤ end }
```

---

## 4. Captain Route Overlay 规范（升级 · 分析层）— 修改十（替换 v1.1 §4）

### 4.1 核心定位（升级）

Captain Route 的本质是**「比赛日顺序分析工具」**——它回答的是「在选定球队集合下，谁先踢、谁后踢、哪些队可切换队长」这一**派生分析问题**。

- **分析层，不是数据层**：它**读取**赛程数据并叠加**高亮 / 角标 / 路径提示**，**绝不写回、不改动**底层数据结构。
- **不改结构**：开启后矩阵的**行、列、单元格结构与位置完全不变**，不重排、不拆分、不折叠、不新增行列。

### 4.2 两条叠加通道（不变，语义随比赛日升级）

1. **矩阵角标**：给有比赛的单元格叠加**开球先后序号角标**，按**北京时刻升序**、以**每个「比赛日」**为排序单元（比赛日由 §1 的 GDID 分组定义）。
2. **浮层 Overlay**：按比赛日列出「开球时间线」与「可切换对」结论。

### 4.3 序号呈现：①②③ 与 Route 1/2/3 的取舍

| 方案 | 适用位置 | 特点 |
| --- | --- | --- |
| **①②③**（圈号） | 矩阵单元格**角标** | 紧凑、视觉轻，适合小尺寸角标；**推荐用于角标** |
| Route 1/2/3 | 浮层时间线的**段落标题** | 文本更显式、可读性强；**推荐用于浮层** |

- 推荐**组合使用**：单元格角标用 `①②③`，浮层时间线用 `①②③` 辅以「Route 1/2/3」作为段落标注，两者指向同一序号体系。
- 序号仍遵循 v1.1 §4.2 规则：**同一比赛日内**按北京时刻升序；跨比赛日连续；同比赛日同时刻多队共享同一序号（并列，如 `③③`）。

### 4.4 路径提示（可切换对）

- **可切换对判定**（沿用 v1.1）：同一比赛日内、开球时刻不同的两队（先踢的做队长，赛后切到后踢的）；跨比赛日按先后顺序衔接。
- 浮层末尾以**路径链**呈现结论：`MCI → BAR → (ARS | PSG) → BAY`。

### 4.5 边界声明（本轮新增）

- Captain Route **不是**一个「新的矩阵」或「第二张表」，而是一个可开关的**叠加分析视图**。
- 关闭开关 → 角标与浮层**同时消失**，矩阵回到纯净态，主数据与结构无任何残留。

---

## 5. Fixture Matrix 扩展规范 — 修改十一

### 5.1 扩展预留原则

Fixture Matrix 未来可能需要承载更多评分维度（见 §5.3）。设计原则如下：

1. **增量追加，不改主体**：扩展信息一律进入单元格的**「扩展槽」**（§2.3 的第三行 / 徽标区），**永不替换**首行 `对手 (H/A)` 与次行时刻。
2. **默认无感**：无扩展数据时扩展槽不渲染、不占空间；有数据时才显示。
3. **统一徽标体系**：所有扩展维度共用同一套徽标/标签样式（`chip`），仅以文案与色相区分语义。
4. **可排序、可关闭**：多个扩展徽标按固定优先级排序；后续可加「显示/隐藏某类扩展」的开关。

### 5.2 扩展槽呈现方案

| 方案 | 示例 | 适用场景 |
| --- | --- | --- |
| **文本徽标** | `NAP (H) [FDR 2]` | 离散数值型（FDR 1~5），语义显式 |
| **星级** | `NAP (H) ★★` | 连续评分型（1~5 星），直觉化 |
| **组合** | `NAP (H) ★★ [ATK 3] [DEF 4]` | 多维度并列，按优先级排序 |

- 徽标统一使用 `chip` 样式（小圆角、浅底、`text-muted` 文案），色相可选配以区分维度（如 FDR 用暖色、实力评分用 `primary`）。
- 徽标放置于单元格第三行，或首行右侧内联（空间紧张时）。

### 5.3 预留扩展维度清单

| 维度 | 缩写/标识 | 含义 | 值域 | 优先级 |
| --- | --- | --- | --- | --- |
| Fixture Difficulty Rating | `FDR` | 对手难度评分 | 1~5（或 ★1~5） | 1 |
| Team Rating | `RAT` / `★★` | 球队综合实力评分 | 1~5 星 | 2 |
| Fixture Score | `FS` | 赛程友好度综合分 | 1~10 | 3 |
| Attack Rating | `ATK` | 进攻端评分 | 1~5 | 4 |
| Defence Rating | `DEF` | 防守端评分 | 1~5 | 4 |

### 5.4 数据模型（扩展字段）

`Match` 对象新增**可选**派生字段（默认空，无数据即不渲染）：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `extensions` | `Array<ExtensionBadge>` | 扩展徽标数组，空数组 = 不显示扩展槽 |
| `ExtensionBadge.key` | string | 维度标识（如 `fdr` / `rating`） |
| `ExtensionBadge.label` | string | 展示文案（如 `FDR 2` / `★★`） |
| `ExtensionBadge.tone` | string | 色相（如 `warm` / `primary` / `muted`） |
| `ExtensionBadge.order` | number | 排序优先级（按 §5.3 优先级） |

---

## 6. 新版 Wireframe（更新后）

### 6.1 Desktop

```
┌────────────────────────────────────────────────────────────────────────────────┐
│  🧅 紫葱酱 Fantasy 赛程规划器                [队长路线 ⚪]  [清空全部]        │
├────────────────────────────────────────────────────────────────────────────────┤
│  比赛日范围：  [ MD1 ▼ ]  →  [ MD8 ▼ ]      ← Matchday Range Filter（新增）  │
│  🔍 [ 搜索球队名或缩写，如 ARS / Arsenal …                         ]        │
│  ▸ 展开 36 队选择（已选 5 / 10）                                             │
│  ┌──────────────┬──────────────┬──────────────┬──────────────┐               │
│  │ ☑ Arsenal ARS│ ☐ Monaco MON │ ☐ Milan MIL  │ ☐ …          │               │
│  └──────────────┴──────────────┴──────────────┴──────────────┘               │
│  已选：[ARS ×] [MCI ×] [BAR ×] [BAY ×] [PSG ×]            （最多 10 支）       │
├────────────────────────────────────────────────────────────────────────────────┤
│  ▼ Matchday 1                      09/09 ~ 09/11（北京时间）                  │
│  ┌──────────────────────────────────────────────────────────────────────────┐ │
│  │ 比赛日    │  ARS  │  MCI  │  BAR  │  BAY  │  PSG  │                     │ │
│  ├──────────┼───────┼───────┼───────┼───────┼───────┤                     │ │
│  │ 比赛日一   │       │POR(A) │       │       │       │                     │ │
│  │ (09/09)  │       │ 03:00 │       │       │       │                     │ │
│  │ 比赛日二   │NAP(A) │       │FEY(H) │       │SBR(H) │ ← ARS/PSG 撞车      │ │
│  │ (09/10)  │ 03:00 │       │ 00:45 │       │ 03:00 │   （同 03:00）       │ │
│  │          │[FDR 4]│       │[FDR 2]│       │       │ ← 扩展槽（可选）      │ │
│  │ 比赛日三   │       │       │       │BOD(H) │       │                     │ │
│  │ (09/11)  │       │       │       │ 03:00 │       │                     │ │
│  └──────────────────────────────────────────────────────────────────────────┘ │
│  ▶ Matchday 2                                                                │
├────────────────────────────────────────────────────────────────────────────────┤
│  数据来源说明 · 非官方 · 仅供 Fantasy 参考 · 时间为北京时间（UTC+8）          │
└────────────────────────────────────────────────────────────────────────────────┘

   队长路线开启后，浮层叠于矩阵右侧（不改变矩阵结构）：
   ┌─ 队长路线 · MD1 ──────┐
   │ Route 1 · 比赛日一      │
   │  ① 03:00 MCI(客POR)    │
   │ Route 2 · 比赛日二      │
   │  ② 00:45 BAR(主FEY)    │
   │  ③ 03:00 ARS(客NAP)    │
   │  ③ 03:00 PSG(主SBR) ⚠ │
   │ Route 3 · 比赛日三      │
   │  ④ 03:00 BAY(主BOD)    │
   │ 可切换：MCI→BAR→       │
   │   (ARS|PSG)→BAY        │
   └───────────────────────┘
```

### 6.2 Mobile（按比赛日的时间线）

```
┌──────────────────────────────┐
│ 🧅 紫葱酱 Fantasy 规划器      │
│ 比赛日范围：[MD1▼]→[MD8▼]     │
│ 🔍 搜索球队…                  │
│ ▸ 展开 36 队选择             │
│ [ARS ×] [MCI ×] [BAR ×] +2  │
├──────────────────────────────┤
│ ▼ Matchday 1  (北京时间)     │
│  ┌─ 比赛日一 · 09/09 ──────┐  │
│  │  MCI  POR (A) · 03:00   │  │
│  ├─ 比赛日二 · 09/10 ──────┤  │
│  │  BAR  FEY (H) · 00:45   │  │
│  │  ARS  NAP (A) · 03:00   │  │  ⚠ 与 PSG 撞车
│  │  PSG  SBR (H) · 03:00   │  │
│  ├─ 比赛日三 · 09/11 ──────┤  │
│  │  BAY  BOD (H) · 03:00   │  │
│  └─────────────────────────┘  │
│ ▶ Matchday 2                 │
├──────────────────────────────┤
│ 队长路线：底部 Sheet 浮层     │
└──────────────────────────────┘
```

---

## 7. 更新后信息架构（IA）

### 7.1 结构树

```
紫葱酱 Fantasy 赛程规划器（单页应用）
│
├── Header（品牌 + 全局操作：队长路线开关 / 清空选择）
├── Matchday Range Filter（新增：起始 MD → 结束 MD，轮级可见范围筛选）
├── Search Area（搜索框，联动 Team Selector 过滤）
├── Team Selector Panel（36 队勾选面板，可折叠，主选队通道）
├── Selected Teams Area（已选球队 Tag 列表）
├── Fixture Matrix Area（核心区）
│   └── Matchday Accordion × N（N = Range Filter 可见轮数）
│       └── Schedule Table（比赛日 × 球队 真表格，行 = 比赛日）
│           └── 单元格扩展槽（FDR / 球队评分 / 赛程评分 / 攻防评分，可选）
│   └── Captain Route Overlay（分析层浮层，叠加不改结构）
└── Footer（数据来源 / 免责声明 / 时区说明）
```

### 7.2 层级关系（更新）

- 维度一（轮）：**Matchday**（1~8），由 Range Filter 控制可见范围。
- 维度二（比赛日）：每个 Matchday 下的**比赛日（GDID 分组）**，是矩阵的**行**维度。
- 维度三（球队）：**Team**（已选 ≤10），是矩阵的**列**维度。
- 比赛（Match）是矩阵单元格内容。
- 开球时刻（Kickoff Time）是**单元格级属性**，用于冲突判定与队长路线排序，不是行级分组维度。
- 扩展评分（FDR / Rating 等）是**单元格扩展槽**的可选内容，增量追加、不改主体。

---

## 8. 变更范围边界（明确「不改」）

- **不改代码**：本次为文档修订，不修改任何 Vite/React 代码。
- **不改 v1.1 已定的主结构**：矩阵仍是「Accordion 容器 + 语义表格」；Team Selector 位置与交互维持 v1.1（折叠面板在搜索区下方），**不在本次 v1.2 范围内调整**。
- **不改数据源**：`fixtures_90_en.json` 结构不动，`gdId` 仅作为归一化层新增的分组键读取，不写回源文件。
- **不改设计系统**：v1.0 §6 视觉理念、§7 色彩、§8 字体、§9 间距、§10/§11 卡片与 Accordion 规范维持不变，仅新增「扩展徽标 chip」与「比赛日行标签」两处样式。
- **不暴露内部键**：GDID 仅存在于数据层与分组逻辑内部，任何界面不得展示（§1.5）。
